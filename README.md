# ICS_TT_Convertor
Takes the multiple entry .ics file (from ISAMS) for one term and converts repeated events into duplicates for the whole year. It automatically takes holidays into account and 2026-27 holidays are set as default but can be changed.
